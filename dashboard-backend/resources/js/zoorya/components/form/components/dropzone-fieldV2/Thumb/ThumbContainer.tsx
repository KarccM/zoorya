import { Box, styled } from "@mui/material";

const ThumbContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
  marginTop: 16,
}))

export default ThumbContainer;